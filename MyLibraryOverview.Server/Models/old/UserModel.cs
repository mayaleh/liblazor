//using System.Web.Security; hash

namespace MyLibraryOverview.Server.Models
{
    public class UserModel
    {
        /*
        private readonly UserContext db = new UserContext();

        public UserAccess GetUserAccessLogin(UserAccess login)
        {
            try
            {
                UserAccess user = db.UserAccess
                    .Where(b => (b.Email == login.Email && b.Password == login.Password))
                    .SingleOrDefault(); 
                               //.Find(bookId);
                               //.Include(d => d.Author);
                return user;
            }
            catch
            {
                throw;
            }
        }

        */
    }
}
